apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: default
  labels:
    app: task-app
    monitoring: spring-boot
spec:
  selector:
    app: task-app
  ports:
    - name: http
      port: 80
      targetPort: 8080